{
  "name": "pdf-editor",
  "version": "1.0.0",
  "description": "PDF Editor — Capacitor Android app (offline PDF.js + jsPDF)",
  "private": true,
  "scripts": {
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "build:android": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "5.7.0",
    "@capacitor/core": "5.7.0"
  },
  "devDependencies": {
    "@capacitor/cli": "5.7.0",
    "typescript": "^5.4.5"
  }
}
