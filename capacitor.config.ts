import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.metusalem969.pdfeditor',
  appName: 'PDF Editor',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
